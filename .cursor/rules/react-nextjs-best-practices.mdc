# React/Next.js 最佳实践规范

适用于 `**/*.tsx`, `**/*.ts` 文件

## 核心规则

### 1. 禁止未使用的导入和变量

编写代码时必须确保所有导入的模块和声明的变量都被使用：

```tsx
// ❌ 错误：导入了未使用的组件
import { Button, Icon, Card } from '@heroui/react' // 如果 Icon 未使用

// ✅ 正确：只导入使用的组件
import { Button, Card } from '@heroui/react'
```

**检查方法**：使用下划线前缀 `_` 标记有意忽略的变量：
```tsx
// 明确表示忽略某个参数
const handleClick = (_event: MouseEvent) => {
  // 不需要使用 event
}
```

### 2. useEffect 中避免同步调用 setState

在 `useEffect` 中直接调用 `setState` 会导致级联渲染，影响性能：

```tsx
// ❌ 错误：在 effect 中同步设置状态
useEffect(() => {
  if (condition) {
    setLoading(false) // 这会触发额外渲染
  }
}, [condition])

// ✅ 正确方案 1：使用 useMemo 计算派生状态
const isLoading = useMemo(() => {
  return !hasHydrated || !isAuthenticated
}, [hasHydrated, isAuthenticated])

// ✅ 正确方案 2：如果必须在 effect 中设置状态，确保有充分理由
useEffect(() => {
  // 仅用于重定向等副作用
  if (hasHydrated && !isAuthenticated) {
    router.replace('/login')
  }
}, [hasHydrated, isAuthenticated, router])
```

### 3. 避免在 try/catch 中构建 JSX

React 不会立即渲染 JSX，所以 try/catch 无法捕获渲染错误：

```tsx
// ❌ 错误：在 try/catch 中返回 JSX
async function Page() {
  try {
    const data = await fetchData()
    return <Component data={data} /> // 渲染错误不会被捕获
  } catch {
    return <ErrorPage />
  }
}

// ✅ 正确：使用 .catch() 处理异步错误，让渲染错误由 Error Boundary 处理
async function Page() {
  const data = await fetchData().catch(() => null)
  
  if (!data) {
    notFound() // 或返回错误组件
  }
  
  return <Component data={data} />
}
```

### 4. 组件导入规范

按以下顺序组织导入：

```tsx
// 1. React 相关
import { useState, useEffect, useMemo } from 'react'

// 2. Next.js 相关
import Link from 'next/link'
import Image from 'next/image'
import { useRouter } from 'next/navigation'

// 3. 第三方 UI 库
import { Button, Card } from '@heroui/react'

// 4. 图标库
import { Sun, Moon } from 'lucide-react'

// 5. 本地组件和工具
import { cn } from '@/lib/utils'
import type { Article } from '@/lib/api/types'
```

### 5. 类型安全

- 使用 TypeScript 严格模式
- 为所有 props 定义接口
- 避免使用 `any` 类型

```tsx
// ✅ 正确：明确的类型定义
interface ArticleCardProps {
  article: Article
  index?: number
  isLatest?: boolean
}

export function ArticleCard({ article, index = 0, isLatest = false }: ArticleCardProps) {
  // ...
}
```

## 编码检查清单

每次完成代码后，确保：

- [ ] 运行 `npm run lint` 无错误
- [ ] 所有导入都被使用
- [ ] 所有变量都被使用（或用 `_` 前缀标记）
- [ ] useEffect 中不直接同步调用 setState
- [ ] 异步操作使用 `.catch()` 而非 try/catch 包裹 JSX
- [ ] TypeScript 无类型错误

## 自动化检查

本项目配置了 ESLint 规则强制执行：
- `@typescript-eslint/no-unused-vars` - 未使用变量报错
- `react-hooks/set-state-in-effect` - useEffect 中 setState 检查
- `react-hooks/error-boundaries` - JSX 在 try/catch 中的检查
